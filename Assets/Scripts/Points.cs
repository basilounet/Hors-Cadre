using UnityEngine;

public class Points : MonoBehaviour
{

    public static float _money = 0;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}